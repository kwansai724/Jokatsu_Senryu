<div class="container">
  <div class="card-deck text-center">
    <div class="card mb-4 shadow-sm">
      <div class="card-header">
        <h4 class="my-0 font-weight-normal">審査員管理者専用ログイン画面</h4>
      </div>
      <div class="card-body">
        <ul class="list-unstyled mt-3 mb-4">
          <li>事前に付与した専用ログインIDでログインしてください。</li>
        </ul>
        <div class="user-signed">
          <div class="signed-in text-start">
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <div class='sign-form'>
                <%= f.label :login_id, class: 'form-label' %>
                <%= f.text_field :login_id, autofocus: true, autocomplete: 'login_id', class: 'form-control', required: true %>
              </div>

              <div class="d-grid gap-2">
                <%= f.submit  t('.sign_in'), class: 'btn btn-primary' %>
              </div>
              
            <% end %>
          </div>
          <div class="voter-staff mt-2">
            <%= link_to '審査員の方はこちら', new_voter_session_path, class: "btn-resize"%>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
