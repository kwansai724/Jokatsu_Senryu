<div class="container">
  <div class="card-deck text-center">
    <div class="card mb-4 shadow-sm">
      <div class="card-header">
        <h4 class="my-0 font-weight-normal">女性活躍川柳　審査員フォーム</h4>
      </div>
      <div class="card-body">
        <h5 class="card-title pricing-card-title">
          ようこそ！<br>
          女性活躍川柳　審査員の皆様<br>
          こちらより審査員の新規登録をお願いします。
        </h5>
        <P>初めての方はこちらの画面で新規登録をしてください。</p>
        <div class="registrations-info">
          <ul class="registrations-info-ul list-unstyled">
            <li>
              ①お名前（ログインIDになります)<br>
              ②あなたの投票グループを選択します<br>
              ③任意のパスワードを入力<br>
              ④［新規登録］ボタンを押す
            </li>
          </ul>
        </div>
        <div class="user-signed">
          <div class="signed-up text-start">
            <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
              <%= bootstrap_devise_error_messages! %>

              <div class='sign-form'>
                <%= f.label :name, class: 'form-label' %>
                <%= f.text_field :name, autofocus: true, autocomplete: 'name', class: 'form-control' %>
              </div>

              <div class='sign-form'>
                <%= f.label :group, class: 'form-label' %>
                <%= f.select :group, Staff.pluck(:group_name), { include_blank: '選択してください'}, autofocus: true, autocomplete: 'group', class: 'form-control' %>
              </div>

              <div class='sign-form'>
                <%= f.label :password, class: 'form-label' %>
                <%= f.password_field :password, autocomplete: 'current-password',
                                                  class: 'form-control',
                                                  required: true,
                                                  minlength: @minimum_password_length,
                                                  maxlength: '30' %>

                <% if @minimum_password_length %>
                  <small class="form-text text-muted"><%= t('devise.shared.minimum_password_length', count: @minimum_password_length) %></small>
                <% end %>
              </div>

              <div class='sign-form'>
                <%= f.label :password_confirmation, class: 'form-label' %>
                <%= f.password_field :password_confirmation, autocomplete: 'current-password', class: 'form-control' %>
              </div>

              <div class="d-grid gap-2">
                <%= f.submit t('.sign_up'), class: 'btn btn-primary' %>
              </div>

            <% end %>
          </div>
          <div class="signed-in d-grid gap-2">
            <%= link_to "ご登録済みの方はこちら", new_voter_session_path, class: 'btn btn-info' %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

