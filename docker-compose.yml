version: '3.9' 
services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_ROOT_HOST: '%'
    ports:
      - '3306:3306'
    command: --default-authentication-plugin=mysql_native_password
    volumes:
      - mysql-data:/var/lib/mysql
  api:
    build: 
      context: .
      args:
        WORKDIR: jokatsu_senryu
    image: jokatsu_senryu/api
    volumes:
      - .:/jokatsu_senryu
      - public:/jokatsu_senryu/public
      - tmp:/jokatsu_senryu/tmp
    depends_on:
      - db
    stdin_open: true
    tty: true
  nginx:
    build:
      args:
        ENVIRONMENT: local
        WORKDIR: jokatsu_senryu  
      context: .
      dockerfile: ./nginx/Dockerfile
    image: jokatsu_senryu/nginx
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - public:/jokatsu_senryu/public
      - tmp:/jokatsu_senryu/tmp
    ports:
      - "80:80"
    depends_on:
      - api
volumes:
  mysql-data:
    driver: local
  tmp:
  public:
